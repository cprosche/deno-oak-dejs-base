# A basic [Deno](https://deno.land/)/[Oak](https://deno.land/x/oak)/[Dejs](https://deno.land/x/dejs) based website.

Start the server on your machine using this command.
```
deno run --allow-net --allow-read server.ts
```